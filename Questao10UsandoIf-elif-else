'''

10.  Dizemos que um número natural é triangular se ele é produto de três números naturais consecutivos.
Exemplo: 120 é triangular, pois 4.5.6 = 120.
Dado um inteiro não-negativo n, verificar se n é triangular.

'''

# Função: questiona se o usuário quer continuar ou sair do programa
def contiousair (cont):
    print('Deseja continuar? Digite 1 para continuar ou 0 para sair.')
    cont = int(input())
    cont = checarsecontehvalido(cont)
    return cont

# Função: verifica se o valor inserido pelo usuário para continuar ou sair do programa é válido
def checarsecontehvalido (cont):
    if cont == 0 or cont == 1:
        # Mensagem exibida ao usuário se ele decidiu sair do programa
        if cont == False:
            print('O exercício foi finalizado.')
    else:
        # Se o usuário tiver inserido número diferente de 0 e 1, apresenta mensagem de erro
        print('Número inválido.')
        # Questiona se o usuário quer continuar ou sair do programa
        contiousair(cont)
    return cont     

def checarvalidadedevalorinserido (num_int_positivo):
    while num_int_positivo < 0:
        # Exibe mensagem de erro se o número inserido for inválido
        print('Número inválido: o número deve ser inteiro não-negativo.')
        # Questiona se o usuário quer continuar ou sair do programa
        cont = contiousair(1)
        # Ação executada se o usuário quer sair do programa
        if cont == 0:
            exit()
        else:
            # Ação executada se o usuário quer continuar - releitura do número
            print('Digite um número inteiro não-negativo:')
            num_int_positivo = int(input())
    return num_int_positivo

# Pede para o usuário informar um número inteiro não-negativo
print('Digite um número inteiro não-negativo:')
# Armazena a quantidade de múltiplos a serem exibidos
n = int(input())

# Verifica se o número n é válido, ou seja, inteiro não-negativo
n = checarvalidadedevalorinserido(n)

# 3 situações gerais para solucionar a questão
# 1a = o número for 0
if n == 0:
  print(n , 'é um número triangular.')
  print('( 0 * 1 * 2 = 0 )')
  # Variável que recebe 1 se a mensagem já foi impressa
  confirma_nt = 1
# 2a = o número for entre 0 e 6
elif n < 6 and n != 0:
  print(n, 'não é um número triangular.')
  # Variável que recebe 1 se a mensagem já foi impressa
  confirma_nt = 1
# 3a = o número for maior ou igual a 6
elif n >= 6:
  result = 0
  nnc = 0
  # Laço enquanto o resultado da multiplicação for menor que o número n
  while nnc < n:
      # Adiciona +1 a cada repetição
    result+=1 
    # Multiplicação dos 3 números consecutivos
    nnc = result*(result+1)*(result+2)
    # Checa se a multiplicação é igual ao número n
    if nnc == n: 
        # Mensagem para o usuário se o número for triangular e qual é a sequência dos 3 números
      print(n, 'é um número triangular.')
      print('(',result,'*',(result+1),'*',(result+2),'=',n,')')
      # Variável que recebe 1 se a mensagem já foi impressa
      confirma_nt = 1
    else:
        # Variável que recebe 0 se nenhuma das mensagens anteriores foi impressa
      confirma_nt = 0
# Mensagem exibida ao usuário se nenhum dos casos anteriores for verdadeiro
# logo, se o número não for triangular      
if confirma_nt == 0:
  print(n, 'não é um número triangular.')
